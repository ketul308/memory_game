import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    padding: {
        padding: "4%"
    },
    btnRestart: {
        marginTop: 20,
        backgroundColor: "#ccc",
        padding: 10,
        borderRadius: 10,
        justifyContent: "center",
        alignItems: "center"
    }
})