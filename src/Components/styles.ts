import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    item: {
        width: "20%",
        height: 100,
        justifyContent: "center",
        alignItems: "center",
        backgroundColor: "#ccc",
        marginLeft: "4%",
        marginTop: "4%"
    },
    textChar: {
        color: "#fff",
        fontSize: 20,
        fontWeight: "bold"
    }
})

export { styles }